<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.cloudidev.syukronApp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="syukronApp"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name="com.cloudidev.syukronApp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.cloudidev.syukronApp.Activity_Jadwal"/>
        <activity android:name="com.cloudidev.syukronApp.Activity_Halal"/>
        <activity android:name="com.cloudidev.syukronApp.Activity_DoaHarian">

        </activity>
        <activity android:label="Dzikir Sore" android:name="com.cloudidev.syukronApp.SoreActivity" />
        <activity android:label="Dzikir Pagi" android:name="com.cloudidev.syukronApp.PagiActivity" />

    </application>

</manifest>